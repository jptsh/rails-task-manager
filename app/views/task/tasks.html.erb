<h1>My Tasks</h1>
  <% @tasks.each do |task| %>
    <div>
        <%= check_box("tag", tag.id, {checked: task.completed}) %>
        <label for="scales"><%= task.title %></label>
    </div>
  <% end %>